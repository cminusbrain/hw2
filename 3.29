#include <iostream>
#include <cmath>

int main()
{
    int x, y, x1, y1, x2, y2;
    std::cin >> x >> y >> x1 >> y1 >> x2 >> y2;
    std::cout << ((x1 < x) & (x < x2) & (y2 < y) & (y < y1));
    return 0;
}
