#include <iostream>
#include <cmath>

int main()
{
    int n;
    std::cin >> n;
    n = fabs(n);
    std::cout << (((n / 100)<(n % 100 / 10)) & ((n % 100 / 10) < (n % 10))) << std::endl;
    return 0;
}
