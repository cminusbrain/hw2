#include <iostream>
#include <cmath>

int main()
{
    int a, b, c;
    std::cin >> a >> b >> c;
    std::cout << ((a * a == b * b + c * c) || (b * b == a * a + c * c) || (c * c == a * a + b * b));
    return 0;
}
